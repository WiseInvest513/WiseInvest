# Wise Invest 项目状态

最后更新：2025-01-XX

## ✅ 已完成的功能

### 1. 工具页面 (`/tools`)
- ✅ 复利计算器 (CompoundInterestCalc)
- ✅ 贪婪恐慌指数 (FearGreedIndex)
- ✅ 空投查询器 (AirdropTracker)
- ✅ 交易所空投追踪 (ExchangeAirdrop)
- ✅ 无常损失计算器 (ImpermanentLoss)
- ✅ APY 计算器 (ApyCalculator)

### 2. 其他页面
- ✅ 推文页面 (`/tweets`) - 带筛选和分页
- ✅ 文集页面 (`/anthology`) - 3栏布局
- ✅ 福利页面 (`/perks`) - 会员福利中心
- ✅ 常用导航页面 (`/resources`) - 资源目录
- ✅ 空投页面 (`/airdrop`) - 空投查询器独立页面
- ✅ 交易所空投页面 (`/airdrop/exchange`) - 交易所空投追踪独立页面

### 3. 全局功能
- ✅ 全局搜索命令面板 (Cmd + K)
- ✅ 新闻订阅 Toast
- ✅ 活动滚动条 (ActivityMarquee)
- ✅ 标准化粘性头部（所有子页面）

## 📁 文件结构

### 组件文件
```
components/
├── tools/
│   ├── CompoundInterestCalc.tsx ✅
│   ├── FearGreedIndex.tsx ✅
│   ├── AirdropTracker.tsx ✅
│   ├── ExchangeAirdrop.tsx ✅
│   ├── ImpermanentLoss.tsx ✅
│   └── ApyCalculator.tsx ✅
├── sections/
│   ├── TweetsSection.tsx ✅
│   ├── ToolsSection.tsx ✅
│   └── AnthologySection.tsx ✅
├── ui/
│   ├── progress.tsx ✅
│   └── ... (其他 Shadcn UI 组件)
├── navbar.tsx ✅
├── footer.tsx ✅
├── newsletter-toast.tsx ✅
└── search-command.tsx ✅
```

### API 路由
```
app/api/
└── airdrop/
    └── exchange/
        └── route.ts ✅
```

### 页面文件
```
app/
├── page.tsx ✅ (首页)
├── tweets/
│   └── page.tsx ✅
├── tools/
│   └── page.tsx ✅
├── anthology/
│   └── page.tsx ✅
├── perks/
│   └── page.tsx ✅
├── resources/
│   └── page.tsx ✅
└── airdrop/
    ├── page.tsx ✅
    └── exchange/
        └── page.tsx ✅
```

### 数据文件
```
lib/
├── data.ts ✅ (推文和工具数据)
├── perks-data.ts ✅ (福利和文集数据)
└── resources-data.ts ✅ (资源数据)
```

## 📦 依赖项

### 已安装的核心依赖
- ✅ next: ^15.0.0
- ✅ react: ^19.0.0
- ✅ react-dom: ^19.0.0
- ✅ tailwindcss: ^3.4.1
- ✅ recharts: ^3.6.0
- ✅ lucide-react: ^0.468.0
- ✅ sonner: ^2.0.7
- ✅ rss-parser: ^3.13.0
- ✅ react-fast-marquee: ^1.6.5
- ✅ html-to-image: ^1.11.13
- ✅ cmdk: ^1.1.1

### UI 组件库
- ✅ @radix-ui/react-dialog
- ✅ @radix-ui/react-tabs

## 🚀 启动项目

```bash
# 安装依赖（如果需要）
npm install --legacy-peer-deps

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start
```

## 🔧 常见问题

### 1. 构建错误
如果遇到构建错误，尝试：
```bash
rm -rf .next
npm run build
```

### 2. 依赖问题
如果依赖安装失败，使用：
```bash
npm install --legacy-peer-deps
```

### 3. 端口占用
如果 3000 端口被占用：
```bash
lsof -ti:3000 | xargs kill -9
npm run dev
```

## 📝 注意事项

1. **Dialog 滚动问题已修复**：所有工具对话框现在使用固定头部和可滚动主体
2. **API 路由**：`/api/airdrop/exchange` 需要网络连接来获取 RSS 数据
3. **本地存储**：空投追踪器的任务完成状态保存在 localStorage
4. **新闻订阅**：订阅状态和关闭状态保存在 localStorage

## 🎯 下一步开发建议

- [ ] 添加更多工具（如投资组合追踪器）
- [ ] 实现真实的 API 集成（替代 mock 数据）
- [ ] 添加用户认证系统
- [ ] 实现数据持久化（数据库）
- [ ] 添加更多空投项目到追踪器
