# Wise Invest - 专业的投资内容平台

一个基于 Next.js 15 构建的专业投资内容平台，提供投资工具、深度分析和优质资源。

## 🚀 快速开始

### 安装依赖

```bash
npm install --legacy-peer-deps
```

### 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看网站。

### 构建生产版本

```bash
npm run build
npm start
```

## 📁 项目结构

```
MyBlog/
├── app/                    # Next.js App Router 页面
│   ├── anthology/         # 文集/知识库页面（3列布局）
│   ├── tools/              # 工具页面
│   ├── tweets/             # 推文列表页面
│   ├── layout.tsx          # 根布局
│   ├── page.tsx            # 首页
│   └── globals.css         # 全局样式
├── components/             # React 组件
│   ├── sections/          # 首页区块组件
│   ├── tools/             # 工具组件
│   ├── ui/                # UI 基础组件（Shadcn）
│   ├── navbar.tsx         # 导航栏
│   ├── footer.tsx         # 页脚
│   └── ...
├── lib/                    # 工具函数和数据
│   ├── data.ts            # 推文和工具数据
│   ├── perks-data.ts      # 知识库数据
│   └── utils.ts           # 工具函数
└── ...
```

## 🎨 技术栈

- **框架**: Next.js 15 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **UI 组件**: Shadcn/UI
- **图标**: Lucide React
- **字体**: Inter (正文), Nunito (标题)
- **数据可视化**: Recharts
- **通知**: Sonner
- **滚动条**: react-fast-marquee
- **图片生成**: html-to-image

## ✨ 主要功能

### 1. 首页 (`/`)
- Hero Section
- 作者介绍
- 投资工具展示
- 最新推文/干货（带分类标签）
- 经典文集
- 推荐资源
- 友站板块
- 活动滚动条（实时活动展示）
- 新闻订阅 Toast（5秒后自动弹出）

### 2. 推文页面 (`/tweets`)
- 专业研究库布局
- 左侧筛选面板（核心赛道 + 内容体裁）
- 右侧表格展示
- 分页功能（10/20条每页）
- 浏览量显示（热门文章🔥标识）
- 响应式设计

### 3. 工具页面 (`/tools`)
- Web3 App Store 风格
- 左侧分类筛选
- 右侧工具卡片网格
- 工具模态框
- 复利计算器（完整功能）

### 4. 文集页面 (`/anthology`)
- 3列专业文档布局
- 左侧：搜索 + 导航树
- 中间：文章内容（max-w-3xl 居中）
- 右侧：目录（Table of Contents）
- 底部：上一篇/下一篇导航
- 实时搜索过滤
- Markdown 内容渲染

## 🎯 设计系统

### 颜色方案
- **主色调**: 黄色 (#FACC15 / bg-yellow-400)
- **背景**: 黑色、白色、灰色（Slate/Zinc）
- **高对比度**: 极简主义风格

### 字体
- **标题**: Nunito (font-heading)
- **正文**: Inter (font-sans)

### 主题
- 支持深色/浅色模式切换
- 使用 `next-themes` 管理主题

## 📦 核心依赖

```json
{
  "next": "^15.0.0",
  "react": "^19.0.0",
  "tailwindcss": "^3.4.1",
  "lucide-react": "^0.468.0",
  "recharts": "^3.6.0",
  "sonner": "^2.0.7",
  "react-fast-marquee": "^1.6.5",
  "html-to-image": "^1.11.13"
}
```

## 🔧 开发说明

### 添加新推文
编辑 `lib/data.ts` 中的 `tweets` 数组。

### 添加新工具
1. 在 `lib/data.ts` 的 `tools` 数组中添加工具信息
2. 如需自定义工具组件，在 `components/tools/` 中创建
3. 在 `app/tools/page.tsx` 中添加条件渲染逻辑

### 添加知识库内容
编辑 `lib/perks-data.ts` 中的 `knowledgeBase` 数组。

### 自定义样式
- 全局样式: `app/globals.css`
- Tailwind 配置: `tailwind.config.ts`
- 主题变量: `app/globals.css` 中的 CSS 变量

## 📝 页面路由

- `/` - 首页
- `/tweets` - 推文列表
- `/tools` - 工具集合
- `/anthology` - 知识库/文集
- `/perks` - 福利（已迁移到 `/anthology`）

## 🎨 UI 组件

使用 Shadcn/UI 组件库：
- Button
- Card
- Dialog
- Tabs
- Badge
- Input
- Label
- Slider
- Table
- Command (搜索命令面板)

## 📱 响应式设计

- 移动端优先
- 断点：sm, md, lg, xl
- 3列布局在大屏显示，小屏自适应

## 🚀 部署

### Vercel（推荐）
1. 推送代码到 GitHub
2. 在 Vercel 中导入项目
3. 自动部署

### 其他平台
```bash
npm run build
npm start
```

## 📄 许可证

私有项目

## 👤 作者

Wise Invest Team

---

**最后更新**: 2025-01-XX
**版本**: 0.1.0
