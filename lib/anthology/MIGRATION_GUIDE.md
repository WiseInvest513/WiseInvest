# 文件夹结构迁移指南

## ✅ 功能已实现

系统现在支持子文件夹结构，可以按作者和分类组织文档。

## 📁 推荐的文件夹结构

```
lib/anthology/documents/
├── buffett/
│   ├── letters/              # 致股东信
│   │   ├── 1980.docx
│   │   └── 2024.docx
│   └── meetings/             # 股东大会
│       ├── 1957-1967股东大会.docx
│       ├── 1968-1978 股东大会.docx
│       ├── 1979-1989 股东大会.docx
│       ├── 1990-2000 股东大会.docx
│       ├── 2001-2010 股东大会.docx
│       ├── 2011-2021 股东大会.docx
│       └── 2022-2025股东大会.docx
└── duan/
    ├── business/              # 商业逻辑
    └── investment/            # 投资语录
```

## 🔄 迁移步骤

### 步骤 1: 创建文件夹结构

```bash
cd lib/anthology/documents

# 创建巴菲特相关文件夹
mkdir -p buffett/letters
mkdir -p buffett/meetings

# 创建段永平相关文件夹
mkdir -p duan/business
mkdir -p duan/investment
```

### 步骤 2: 移动现有文件

```bash
# 移动股东大会文档
mv "1957-1967股东大会.docx" buffett/meetings/
mv "1968-1978 股东大会.docx" buffett/meetings/
mv "1979-1989 股东大会.docx" buffett/meetings/
mv "1990-2000 股东大会.docx" buffett/meetings/
mv "2001-2010 股东大会.docx" buffett/meetings/
mv "2011-2021 股东大会.docx" buffett/meetings/
mv "2022-2025股东大会.docx" buffett/meetings/
```

### 步骤 3: 更新 metadata.ts（可选）

系统会自动根据 ID 推断路径，但如果你想明确指定路径，可以在 metadata.ts 中添加：

```typescript
{
  id: "1957-1967股东大会",
  title: "1957-1967股东大会",
  path: "buffett/meetings/1957-1967股东大会.docx"  // 可选
}
```

## 🎯 自动路径推断规则

系统会根据文章 ID 自动推断路径，无需手动配置：

| ID 格式 | 推断路径 |
|---------|---------|
| `buffett-letter-1980` | `buffett/letters/1980.docx` |
| `*股东大会` | `buffett/meetings/{id}.docx` |
| `duan-business-*` | `duan/business/{id}.docx` |
| `duan-invest-*` | `duan/investment/{id}.docx` |
| 其他 | `{id}.docx` (向后兼容) |

## 🔙 向后兼容

系统完全向后兼容：
- 如果文件在新路径不存在，会自动尝试旧路径（根目录）
- 旧文件可以继续使用，无需立即迁移

## 📝 添加新文档

### 方式1: 使用自动推断（推荐）

只需将文件放到对应文件夹，ID 符合命名规则即可：

```bash
# 添加新的致股东信
cp 1981年致股东信.docx lib/anthology/documents/buffett/letters/1981.docx
```

在 metadata.ts 中添加：
```typescript
{ id: "buffett-letter-1981", title: "1981年致股东信" }
```

### 方式2: 手动指定路径

如果文件名不符合规则，可以在 metadata.ts 中指定路径：

```typescript
{
  id: "custom-id",
  title: "自定义标题",
  path: "buffett/letters/custom-file.docx"
}
```

## ✅ 验证

迁移后，重启服务器并访问 `/anthology` 页面，确认所有文档都能正常加载。
