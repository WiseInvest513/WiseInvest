# Git 提交指南

本文档详细介绍如何将代码提交到 GitHub 仓库。

## 📋 目录

1. [准备工作](#准备工作)
2. [首次提交（新仓库）](#首次提交新仓库)
3. [日常提交流程](#日常提交流程)
4. [提交信息规范](#提交信息规范)
5. [常见问题](#常见问题)
6. [最佳实践](#最佳实践)

---

## 🔧 准备工作

### 1. 检查 Git 是否已安装

```bash
git --version
```

如果未安装，请访问：https://git-scm.com/downloads

### 2. 配置 Git 用户信息（如果还未配置）

```bash
# 设置用户名
git config --global user.name "Your Name"

# 设置邮箱
git config --global user.email "your.email@example.com"

# 查看配置
git config --global --list
```

### 3. 检查当前 Git 状态

```bash
# 进入项目目录
cd /Users/balala/Persona/Blog/MyBlog

# 查看 Git 状态
git status
```

---

## 🚀 首次提交（新仓库）

### 步骤 1: 初始化 Git 仓库（如果还没有）

```bash
# 如果还没有初始化 Git 仓库
git init
```

### 步骤 2: 创建 .gitignore 文件（如果还没有）

确保 `.gitignore` 文件包含以下内容：

```gitignore
# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
logs
*.log

# Cache
.cache/
.parcel-cache/
```

### 步骤 3: 添加所有文件到暂存区

```bash
# 添加所有文件
git add .

# 或者只添加特定文件
git add app/tools/contract-calculator/
git add components/tools/ContractCalculator.tsx
git add lib/data.ts
git add lib/tool-routes.ts
```

### 步骤 4: 创建首次提交

```bash
git commit -m "feat: 添加合约交易计算器功能

- 新增合约交易计算器组件
- 支持计算保证金、盈亏、强平价格
- 添加风险提示和说明文档
- 更新工具列表和路由配置"
```

### 步骤 5: 连接到 GitHub 远程仓库

#### 5.1 在 GitHub 上创建新仓库

1. 访问 https://github.com/new
2. 输入仓库名称（例如：`MyBlog`）
3. 选择 Public 或 Private
4. **不要**勾选 "Initialize this repository with a README"
5. 点击 "Create repository"

#### 5.2 添加远程仓库地址

```bash
# 添加远程仓库（替换 YOUR_USERNAME 和 YOUR_REPO_NAME）
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 或者使用 SSH（推荐）
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO_NAME.git

# 查看远程仓库配置
git remote -v
```

### 步骤 6: 推送到 GitHub

```bash
# 推送到主分支（首次推送）
git push -u origin main

# 如果默认分支是 master，使用：
git push -u origin master
```

---

## 📝 日常提交流程

### 标准提交流程（3 步）

```bash
# 1. 查看更改状态
git status

# 2. 添加更改的文件到暂存区
git add .

# 或者只添加特定文件
git add app/tools/contract-calculator/
git add components/tools/ContractCalculator.tsx

# 3. 提交更改
git commit -m "feat: 添加合约交易计算器功能"

# 4. 推送到 GitHub
git push
```

### 查看更改内容

```bash
# 查看所有更改
git diff

# 查看已暂存的更改
git diff --staged

# 查看提交历史
git log --oneline

# 查看特定文件的更改历史
git log --oneline -- app/tools/contract-calculator/page.tsx
```

---

## 📋 提交信息规范

### 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型说明

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整（不影响功能）
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

### 提交信息示例

#### 示例 1: 新功能

```bash
git commit -m "feat(contract-calculator): 添加合约交易计算器

- 支持计算保证金、盈亏、强平价格
- 添加做多/做空强平价格计算
- 包含风险提示和说明文档"
```

#### 示例 2: 修复 Bug

```bash
git commit -m "fix(contract-calculator): 修复保证金计算除以零错误

- 添加杠杆倍数为零的边界检查
- 移除未使用的 Button 组件导入"
```

#### 示例 3: 功能更新

```bash
git commit -m "feat(macro-dashboard): 重构宏观仪表板数据获取

- 使用 yahoo-finance2 库获取实时数据
- 修复 10年期国债收益率显示错误（99% -> 4.25%）
- 修复 VIX 数据映射错误
- 添加数据验证和错误处理"
```

#### 示例 4: 删除功能

```bash
git commit -m "chore: 删除彩虹图功能

- 移除 rainbow-chart 相关页面和组件
- 删除 API 路由文件
- 更新工具列表和路由配置"
```

---

## 🔍 常见问题

### 1. 如何撤销最后一次提交？

```bash
# 撤销提交但保留更改（可以重新提交）
git reset --soft HEAD~1

# 撤销提交并丢弃更改（谨慎使用）
git reset --hard HEAD~1
```

### 2. 如何修改最后一次提交信息？

```bash
git commit --amend -m "新的提交信息"
```

### 3. 如何撤销已暂存的文件？

```bash
# 撤销所有已暂存的文件
git reset

# 撤销特定文件
git reset HEAD <文件名>
```

### 4. 如何查看远程仓库信息？

```bash
# 查看远程仓库
git remote -v

# 查看远程分支
git branch -r

# 查看所有分支（本地+远程）
git branch -a
```

### 5. 如何拉取远程更改？

```bash
# 拉取并合并远程更改
git pull origin main

# 或者先拉取再合并
git fetch origin
git merge origin/main
```

### 6. 如何解决冲突？

```bash
# 1. 拉取远程更改
git pull origin main

# 2. 如果有冲突，Git 会提示
# 3. 打开冲突文件，手动解决冲突
# 4. 标记冲突已解决
git add <冲突文件>

# 5. 完成合并
git commit -m "merge: 解决合并冲突"
```

### 7. 如何创建新分支？

```bash
# 创建并切换到新分支
git checkout -b feature/new-feature

# 或者使用新语法
git switch -c feature/new-feature

# 推送到远程
git push -u origin feature/new-feature
```

---

## ✅ 最佳实践

### 1. 提交前检查清单

- [ ] 代码可以正常运行
- [ ] 没有 console.log 调试代码
- [ ] 没有未使用的导入
- [ ] 没有 linter 错误
- [ ] 提交信息清晰明确

### 2. 提交频率建议

- ✅ **推荐**: 完成一个功能或修复一个 bug 后立即提交
- ✅ **推荐**: 每天至少提交一次（即使工作未完成）
- ❌ **不推荐**: 累积大量更改后一次性提交
- ❌ **不推荐**: 提交无法运行的代码

### 3. 提交信息建议

- ✅ 使用中文或英文，保持一致
- ✅ 第一行简洁描述（50 字以内）
- ✅ 详细说明在 body 中（可选）
- ✅ 说明为什么更改，而不仅仅是改了什么

### 4. 文件管理建议

- ✅ 使用 `.gitignore` 忽略不需要的文件
- ✅ 不要提交 `node_modules/`、`.env` 等敏感文件
- ✅ 定期清理未跟踪的文件

---

## 📦 本次会话的提交建议

基于本次会话的更改，建议使用以下提交信息：

### 提交 1: 新增合约计算器

```bash
git add app/tools/contract-calculator/
git add components/tools/ContractCalculator.tsx
git add lib/data.ts
git add lib/tool-routes.ts

git commit -m "feat: 添加合约交易计算器

- 新增合约交易计算器组件，支持计算保证金、盈亏、强平价格
- 支持做多/做空强平价格计算
- 添加风险提示和详细说明
- 更新工具列表和路由配置"
```

### 提交 2: 修复宏观仪表板

```bash
git add app/api/macro/route.ts
git add app/tools/macro-dashboard/page.tsx
git add app/api/price/route.ts

git commit -m "fix(macro-dashboard): 修复宏观仪表板数据获取错误

- 使用 yahoo-finance2 库重构数据获取逻辑
- 修复 10年期国债收益率显示错误（99% -> 正确百分比）
- 修复 VIX 数据映射错误
- 添加数据验证和错误处理
- 优化为生产就绪版本，适用于 Vercel 部署"
```

### 提交 3: 优化加密货币收益率对比

```bash
git add app/tools/god-mode/crypto-yields/page.tsx
git add components/god-mode/RoiChart.tsx

git commit -m "feat(crypto-yields): 优化加密货币收益率对比功能

- 修复对比对话框图表显示问题
- 优化图表和表格之间的间距
- 启用 tooltip 显示详细数据
- 改进图表渲染和布局"
```

### 提交 4: 删除彩虹图功能

```bash
git add lib/data.ts
git add lib/tool-routes.ts

git commit -m "chore: 删除彩虹图功能

- 移除 rainbow-chart 相关页面和组件
- 删除 API 路由文件
- 更新工具列表和路由配置"
```

---

## 🚀 快速开始命令

### 一次性提交所有更改

```bash
# 查看更改
git status

# 添加所有更改
git add .

# 提交（使用通用信息）
git commit -m "feat: 更新项目功能

- 添加合约交易计算器
- 修复宏观仪表板数据获取
- 优化加密货币收益率对比
- 删除彩虹图功能"

# 推送到 GitHub
git push
```

---

## 📚 参考资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 指南](https://guides.github.com/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Git 最佳实践](https://github.com/git/git/blob/master/Documentation/SubmittingPatches)

---

## 💡 提示

1. **定期提交**: 养成频繁提交的习惯，避免丢失工作
2. **清晰的信息**: 提交信息要清晰，方便以后查找和理解
3. **小步提交**: 每次提交只包含相关的更改
4. **测试后再提交**: 确保代码可以正常运行
5. **使用分支**: 对于大型功能，使用分支开发后再合并

---

**最后更新**: 2025-01-XX
**维护者**: Wise Invest Team
