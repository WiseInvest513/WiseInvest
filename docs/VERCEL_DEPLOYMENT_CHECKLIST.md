# Vercel 部署检查清单

## ✅ 部署前检查

### 1. 代码准备

- [x] 代码已推送到 GitHub
- [x] `package.json` 包含正确的构建脚本
- [x] `next.config.ts` 配置正确
- [x] `vercel.json` 已配置（包含 `--legacy-peer-deps`）
- [x] `.gitignore` 包含不需要的文件

### 2. 构建测试

在本地测试构建：

```bash
# 清理之前的构建
npm run clean

# 安装依赖
npm install --legacy-peer-deps

# 构建项目
npm run build

# 如果构建成功，说明可以部署
```

### 3. 配置文件检查

#### package.json
- ✅ `build` 脚本: `next build`
- ✅ `start` 脚本: `next start`
- ✅ `engines` 字段: 指定 Node.js 版本

#### vercel.json
- ✅ `installCommand`: `npm install --legacy-peer-deps`
- ✅ `buildCommand`: `npm run build`
- ✅ `framework`: `nextjs`

#### next.config.ts
- ✅ 生产环境配置正确
- ✅ TypeScript 和 ESLint 检查启用

---

## 🚀 部署步骤

### 步骤 1: 登录 Vercel

1. 访问 https://vercel.com
2. 使用 GitHub 账号登录

### 步骤 2: 导入项目

1. 点击 **"Add New..."** → **"Project"**
2. 选择你的 GitHub 仓库
3. 点击 **"Import"**

### 步骤 3: 配置项目（重要）

#### 3.1 Framework Preset
- ✅ 自动检测为 **Next.js**（无需修改）

#### 3.2 Build and Output Settings
- ✅ **Build Command**: `npm run build`（自动填充）
- ✅ **Output Directory**: `.next`（自动填充）
- ✅ **Install Command**: `npm install --legacy-peer-deps`（已在 vercel.json 中配置）

#### 3.3 Root Directory
- ✅ 保持默认 `./`（项目在根目录）

#### 3.4 Environment Variables（如果需要）
- 添加必要的环境变量
- 例如：`NEXT_PUBLIC_API_URL`

### 步骤 4: 部署

1. 点击 **"Deploy"** 按钮
2. 等待构建完成（1-3 分钟）
3. 查看构建日志，确认无错误

---

## 🔍 部署后验证

### 1. 检查部署状态

- [ ] 部署状态为 **"Ready"**（绿色）
- [ ] 没有构建错误
- [ ] 没有运行时错误

### 2. 功能测试

访问部署的网站，测试以下功能：

- [ ] 首页正常加载
- [ ] 导航栏工作正常
- [ ] 工具页面可以访问
- [ ] API 路由正常工作（例如：`/api/macro`）
- [ ] 动态路由正常工作
- [ ] 样式和主题切换正常

### 3. API 测试

测试关键 API：

```bash
# 测试宏观数据 API
curl https://your-project.vercel.app/api/macro

# 测试价格 API
curl https://your-project.vercel.app/api/price?type=crypto&action=price&symbol=BTC
```

---

## 🐛 常见问题排查

### 问题 1: 构建失败 - 依赖安装错误

**症状**: `npm ERR! peer dep missing`

**解决**:
- ✅ 已在 `vercel.json` 中配置 `installCommand: "npm install --legacy-peer-deps"`
- 如果仍然失败，在 Vercel 项目设置中手动设置 Install Command

### 问题 2: 构建失败 - TypeScript 错误

**症状**: `Type error: ...`

**解决**:
- 检查 `tsconfig.json` 配置
- 确保所有类型定义正确
- 临时解决方案：在 `next.config.ts` 中设置 `typescript.ignoreBuildErrors: true`（不推荐）

### 问题 3: 运行时错误 - API 路由 500

**症状**: API 返回 500 错误

**解决**:
1. 查看 Vercel 函数日志（项目 → Functions → Logs）
2. 检查 API 路由代码
3. 确保服务器端代码正确

### 问题 4: 页面空白或样式丢失

**症状**: 页面加载但显示异常

**解决**:
- 检查 `globals.css` 是否正确导入
- 检查 Tailwind CSS 配置
- 清除浏览器缓存

---

## 📊 监控和维护

### 1. 查看部署日志

1. 进入项目页面
2. 点击 **"Deployments"**
3. 点击任意部署记录
4. 查看 **"Build Logs"** 和 **"Function Logs"**

### 2. 性能监控

- 使用 Vercel Analytics（可选）
- 查看 Core Web Vitals
- 监控 API 响应时间

### 3. 错误监控

- 集成 Sentry（可选）
- 查看 Vercel 函数错误日志
- 设置错误告警

---

## 🔄 更新部署

### 自动部署（推荐）

推送到 GitHub 的 `main` 分支会自动触发部署：

```bash
git add .
git commit -m "feat: 更新功能"
git push origin main
```

### 手动部署

如果需要手动触发：

1. 进入 Vercel 项目页面
2. 点击 **"Deployments"**
3. 点击 **"Redeploy"** → **"Use Existing Build"** 或 **"Create New Build"**

---

## ✅ 部署成功标志

部署成功后，你应该看到：

- ✅ 部署状态：**Ready**（绿色）
- ✅ 访问 URL：`https://your-project.vercel.app`
- ✅ 所有页面正常加载
- ✅ API 路由正常工作
- ✅ 没有控制台错误

---

## 📝 部署后任务

1. **测试所有功能**
   - 浏览各个页面
   - 测试所有工具
   - 验证 API 功能

2. **配置自定义域名**（可选）
   - Settings → Domains
   - 添加你的域名
   - 配置 DNS 记录

3. **设置环境变量**（如果需要）
   - Settings → Environment Variables
   - 添加生产环境变量

4. **启用 Analytics**（可选）
   - Settings → Analytics
   - 启用 Vercel Analytics

---

**最后更新**: 2025-01-XX
**维护者**: Wise Invest Team
